apiVersion: v1
kind: Service
metadata:
  name: {{SERVICE_NAME}}
  namespace: enlight-staging
  labels:
    app: {{SERVICE_NAME}}
spec:
  selector:
    app: {{SERVICE_NAME}}
  ports:
    - name: http
      port: 80
      targetPort: 8000
