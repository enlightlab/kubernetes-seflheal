# Production: replace with ExternalSecrets Operator + vault/AWS SM
apiVersion: v1
kind: Secret
metadata:
  name: {{SERVICE_NAME}}-secrets
  namespace: enlight-staging
  labels:
    app: {{SERVICE_NAME}}
type: Opaque
stringData:
  API_KEY: "from-vault-in-production"
  DATABASE_PASSWORD: "from-vault-in-production"
