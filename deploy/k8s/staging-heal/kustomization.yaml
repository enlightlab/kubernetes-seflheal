apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: enlight-staging

# Full valid manifests — do NOT apply a partial Deployment (breaks spec.selector).
resources:
  - ../staging-app
